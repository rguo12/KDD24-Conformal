# 对于所有的参数类型，可以参考这里
# 模板的参数
params:
  - name: dataset
    value: 'ihdp'
 
# 对于所有的输入输出类型，可以参考这里
# 模板的输入
# inputs:
#   train_dataset:
#     id: 47cb00a5d0b617f5
#   test_dataset:
#     id: 883f0adf0291cb7e

# 模板的输出
# outputs:
#   output_folder:
#     caption: hongkai.train.mnist-MODEL_copy_1-MODEL
#     id: '@new'
 
resource:
  arnoldConfig:
    cluster_name: 'cloudnative-lq'
    group_names: ['labvc_bigmem']
    roles:
      - name: worker
        num: 1
        memory: 8192
        gpu: 0
        gpuv: NONE
        cpu: 4